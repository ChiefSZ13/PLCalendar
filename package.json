{
  "name": "prairielearn-calendar",
  "version": "0.3.0",
  "private": true,
  "description": "Private PrairieLearn deadline sync for Apple Calendar",
  "scripts": {
    "start": "node companion/server.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check extension/core.js && node --check extension/content.js && node --check extension/background.js && node --check extension/popup.js && node --check companion/server.mjs"
  }
}
